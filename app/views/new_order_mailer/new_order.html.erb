<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>New Order Received</title>
  <style>
    body { font-family: Arial, sans-serif; background-color: #f4f4f4; padding: 20px; }
    .container { background: #ffffff; padding: 20px; border-radius: 8px; max-width: 600px; margin: auto; box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1); }
    .header { font-size: 22px; color: #333; text-align: center; font-weight: bold; }
    .details { margin: 20px 0; padding: 15px; background: #f9f9f9; border-radius: 5px; }
    .button { display: inline-block; padding: 10px 15px; background: #630CDB; color: #ffffff; text-decoration: none; border-radius: 5px; font-weight: bold; }
  </style>
</head>
<body>
  <div class="container">
    <p class="header">🎉 New Order Received!</p>

    <p>Congratulations! A customer has placed an order for your product:</p>

    <div class="details">
      <% @order_items.each do |order_item| %>
        <p>🛍 <strong>Product:</strong> <%= "#{order_item.product.name} #{order_item.product.variant}" %></p>
        <p>📦 <strong>Quantity:</strong> <%= order_item.quantity %></p>
      <% end %>
      <p>💰 <strong>Total Amount:</strong> $<%= @amount %></p>
      <p>⏳ <strong>Order Time:</strong> <%= @time %></p>
    </div>

    <p>Please process the order promptly to ensure timely delivery and customer satisfaction.</p>

    <p style="text-align: center; color: #ffffff ;">
      <a href="<%= @url %>" class="button" style="color: #ffffff">📦 View Order Details</a>
    </p>

    <p>Thank you for selling with us!</p>

  </div>
</body>
</html>
