<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PDF</title>
    <link rel="stylesheet" href="dashboard.scss" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        .setRecentOrderTble {
            margin-top: 25px;
        }

        .setRecentOrderTble thead th {
            border: none;
            color: black;
            font-size: 14px;
            font-weight: 600;
            font-family: "Nunito Sans";
            text-align: center;
        }

        .setRecentOrderTble td,
        .setRecentOrderTble th {
            border: none;
        }

        .setBorderBottomTr {
            border-bottom: 0.4px solid rgba(151, 151, 151, 1)
        }

        .tHeadBgColor {
            background: rgba(241, 244, 249, 1);
        }

        .setFontCartTable {
            color: #4d4e50;
            font-size: 12px;
            font-weight: 500;
            font-family: "Nunito Sans";
        }

        .setBorderBottomTr td {
            padding: 25px 0px;
            text-align: center;
        }

    
        .true{
            color: #fff;
            background: rgba(0, 182, 155, 1);
            border-radius: 15px;
            padding: 5px 10px;
            font-weight: 700;
     }
        .false{
            color: #fff;
            background: rgb(190, 3, 81);;
            border-radius: 15px;
            padding: 5px 10px;
            font-weight: 700;
      }
    </style>
</head>

<body>
    <h1> Order Details </h1>

    <table class="table setRecentOrderTble text-center mt-5" id="cart-table">
        <thead class="tHeadBgColor">
            <tr>
                <th>Number</th>
                <th>Seller</th>
                <th>Product Name</th>
                <th>Sku</th>
                <th>Brand</th>
                <th>Device</th>
                <th>Model</th>
                <th>Quantity</th>
                <th>Bidded</th>
                <th>Price</th>
                <th>Total Cost</th>
            </tr>
        </thead>
        <tbody class="setFontCartTable">
            <% total_price = 0 %>
            <% @order.order_items.each.with_index do |order_item, index| %>
            <tr class="setBorderBottomTr">
                <td><%= index +1  %></td>
                <td><%= order_item.inventory.seller.full_name %></td>
                <td><%= order_item.product.name %></td>
                <td><%= order_item.product.sku %></td>
                <td><%= order_item.product.brand.name %></td>
                <td><%= order_item.product.device.name %></td>
                <td><%= order_item.product.category.name %></td>
                <td class="d-flex align-items-center justify-content-center">
                    <%= order_item.quantity %>
                </td>
                <td > <span class="<%= @order.was_bid? ? 'false' : 'false' %>"> <%= @order.was_bid %> </span></td>
                <td> $<%= order_item.inventory.price %></td>
                <td>$<%= order_item.inventory.price * order_item.quantity %></td>
                <% total_price += order_item.inventory.price * order_item.quantity %>
            </tr>
            <% end %>
            <tr class="totalTextSet">
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td>Total = $<%=  total_price%> </td>
            </tr>
        </tbody>
    </table>
    </div>
</body>

</html>