<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>PDF</title>
    <link rel="stylesheet" href="dashboard.scss" />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <style>
        .setRecentOrderTble {
            margin-top: 25px;
        }

        .setRecentOrderTble thead th {
            border: none;
            color: black;
            font-size: 14px;
            font-weight: 700;
            font-family: "Nunito Sans";
            text-align: center;
        }

        .setRecentOrderTble td,
        .setRecentOrderTble th {
            border: none;
        }

        .setBorderBottomTr {
            border-bottom: 0.4px solid rgba(151, 151, 151, 1)
        }

        .tHeadBgColor {
            background: rgba(241, 244, 249, 1);
        }

        .setRecentOrderTble thead th:first-child {
            border-radius: 10px 0 0 10px;
        }

        .setRecentOrderTble thead th:last-child {
            border-radius: 0 10px 10px 0;
        }

        .setFontOrderTable {
            color: #4d4e50;
            font-size: 14px;
            font-weight: 500;
            font-family: "Nunito Sans";
        }

        .setBorderBottomTr td {
            padding: 25px 0px;
            text-align: center;
        }

        .setBorderBottomTr td span {
            background: rgba(0, 182, 155, 1);
            color: white;
            border-radius: 15px;
            padding: 5px 10px;
        }
    </style>
</head>

<body>
    <h1> Order Details </h1>

    <table class="table setRecentOrderTble text-center mt-5" id="cart-table">
        <thead class="tHeadBgColor">
            <tr>
                <th>Number</th>
                <th>Description</th>
                <th>Stock Available</th>
                <th>Quantity</th>
                <th>Base Cost</th>
                <th>Total Cost</th>
            </tr>
        </thead>
        <tbody class="setFontCartTable">
            <% total_price = 0 %>
            <% @cart.cart_items.each.with_index do |cart_item, index| %>
            <tr class="setBorderBottomTr">
                <td><%= index +1  %></td>
                <td><%= cart_item.product.name %></td>
                <td><%= cart_item.inventory.stock_quantity %></td>
                <td class="d-flex align-items-center justify-content-center">
                    <%= cart_item.quantity %>
                </td>
                <td> $<%= cart_item.inventory.price %></td>
                <td>$<%= cart_item.inventory.price * cart_item.quantity %></td>
                <% total_price += cart_item.inventory.price * cart_item.quantity %>
            </tr>
            <% end %>
            <tr class="totalTextSet">
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td>Total = $<%=  total_price%> </td>
            </tr>
        </tbody>
    </table>
    </div>
</body>

</html>